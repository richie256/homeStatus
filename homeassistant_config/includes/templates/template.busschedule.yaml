- platform: template
  sensors:
    alarm_summary:
      friendly_name: 'Bus Prochain Arrêt'
      value_template: > 
        dans {{ states('busschedule_nextstop_nbrmins') }}mins, {{ states('busschedule_nextstop_nbrsecs') }}secs.
- platform: template
  sensors:
    alarm_summary:
      friendly_name: 'Bus Numéro'
      value_template: > 
        bus {{ states('busschedule_route_id') }}
